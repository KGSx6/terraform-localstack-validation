## 🚀 CI/CD — Despliegue automático
![Terraform Deploy](https://github.com/KGSx6/terraform-localstack-validation/actions/workflows/deploy.yml/badge.svg)

Este flujo ejecuta automáticamente `terraform init`, `plan` y `apply` sobre **LocalStack** cada vez que haces `git push`.

